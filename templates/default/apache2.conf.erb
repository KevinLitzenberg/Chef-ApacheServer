# Managed by Chef for <%= node['hostname'] %>



Alias / <%= node['apache_php']['document_root'] %>/

#This is where Virtual Hosts will go
<Directory <%= node['apache_php']['document_root'] %>>
	#Taken from the apache2.conf file created by apache
	Options Indexes FollowSymLinks
	AllowOverride None
	Require all granted

</Directory>

